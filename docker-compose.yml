name: dml-prod

services:
  dml-prod-app:
    build: .
    container_name: domorelabs.in
    restart: always
    env_file:
      - .env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dml-prod.rule=Host(`domorelabs.in`) || Host(`www.domorelabs.in`)"
      - "traefik.http.routers.dml-prod.tls=true"
      - "traefik.http.routers.dml-prod.tls.certresolver=lets-encrypt"
      - "traefik.http.services.dml-prod.loadbalancer.server.port=8000"
    networks:
      dml-net:
        ipv4_address: 172.40.0.10

networks:
  dml-net:
    driver: bridge
    name: dml-prod-net
    ipam:
      config:
        - subnet: 172.40.0.0/24
